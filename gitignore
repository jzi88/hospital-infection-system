# Environment variables (contains Supabase keys — never commit these)
.env
.env.local
hospital-ml/.env

# Node.js
node_modules/
npm-debug.log*

# Python
hospital-ml/venv/
hospital-ml/__pycache__/
hospital-ml/*.pyc

# YOLO downloaded weights (large, auto-downloads on first run anyway)
hospital-ml/yolov8n.pt

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
